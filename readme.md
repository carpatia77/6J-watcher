# 6J Watcher Stream Integration

Run:

```bash
pip install -r requirements.txt
python main.py
```

Set your ClusterDelta subscription in MetaTrader, then use the MQL bridge to emit JSON lines for the Python side.
